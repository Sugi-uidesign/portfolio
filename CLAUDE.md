# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

モダンで先進的なUI/UXのぷよぷよ（Puyo Puyo）ゲームアプリ。Next.js 16 (App Router) + TypeScript + Tailwind CSS v4 で構築。

## Commands

```bash
npm run dev      # 開発サーバー起動 (http://localhost:3000)
npm run build    # 本番ビルド
npm run start    # 本番サーバー起動
npm run lint     # ESLint 実行
```

## Tech Stack

- **Next.js 16.2** — App Router, React Server Components
- **React 19** — クライアントサイドゲームロジック (`"use client"` 必須)
- **Tailwind CSS v4** — `@import "tailwindcss"` 方式（`tailwind.config.js` 不要）
- **TypeScript 5**

## Architecture

ゲームロジックはすべてクライアントサイドで動作する。

```
src/
  app/
    layout.tsx        # ルートレイアウト（Geist フォント設定済み）
    page.tsx          # ゲームのエントリーポイント
    globals.css       # Tailwind v4 import + CSS 変数
  components/
    game/             # ゲーム関連コンポーネント（"use client"）
  hooks/
    useGameState.ts   # ゲームステート管理（useReducer ベース）
  lib/
    puyo/             # ぷよぷよのコアロジック（純粋関数）
      board.ts        # ボード操作・衝突判定
      gravity.ts      # 落下・連鎖ロジック
      chain.ts        # 連鎖計算・消去判定
      types.ts        # 型定義
```

### 設計方針

- **ゲームロジックとUIの分離**: `src/lib/puyo/` にフレームワーク非依存の純粋関数を置き、コンポーネントからは hooks 経由で呼び出す
- **状態管理**: `useReducer` + `useContext` でグローバルゲームステートを管理（外部ライブラリ不使用）
- **アニメーション**: CSS アニメーション / Tailwind transition を優先、必要に応じて Web Animations API
- **キーボード入力**: `useEffect` 内で `keydown` イベントリスナーを登録

---

## 要件定義

### 1. ゲームボード

- **サイズ**: 6列 × 12行（標準ぷよぷよ仕様）
- 上部に2行の見えない「死亡判定ゾーン」を設ける（ぷよが積み上がった際のゲームオーバー判定用）

### 2. ぷよ

- **色**: 赤・青・緑・黄・紫の5色
- 2つ1組（ツモ）で上部中央から出現
- 上のぷよ（軸ぷよ）と下のぷよ（子ぷよ）で構成
- お邪魔ぷよ（灰色・消去不可）は連鎖数に応じて降ってくる（対戦モード実装時）

### 3. 操作

| 操作 | キーボード | モバイル |
|------|-----------|--------|
| 左移動 | `←` / `A` | スワイプ左 |
| 右移動 | `→` / `D` | スワイプ右 |
| 左回転 | `Z` / `Q` | タップ左 |
| 右回転 | `X` / `E` | タップ右 |
| 高速落下 | `↓` / `S` | スワイプ下 |
| 即時落下 | `Space` | ダブルタップ |

### 4. ゲームロジック

- **落下**: 一定間隔（レベルに応じて加速）でぷよが1マス落下
- **接地**: ぷよが床または他のぷよに接触したら固定
- **消去条件**: 同色ぷよが上下左右に4つ以上連結で消去
- **連鎖**: 消去後にぷよが落下し、再度消去条件を満たす場合に連鎖カウント
- **連鎖ボーナス**: 連鎖数・消去数・色数に応じてスコアに倍率をかける
- **ネクストぷよ**: 次の2組（NEXTx2）を表示
- **ゲームオーバー**: 出現位置（上部中央）がぷよでふさがれたら終了

### 5. スコアリング

```
基本得点 = 消去ぷよ数 × 10
ボーナス = (連鎖ボーナス + 色ボーナス + 連結ボーナス)
合計得点 = 基本得点 × max(ボーナス, 1)
```

- **連鎖ボーナス**: 1連鎖=0, 2連鎖=8, 3連鎖=16, 4連鎖=32, 以降2倍ずつ
- **色ボーナス**: 2色=3, 3色=6, 4色=12, 5色=24
- **連結ボーナス**: 4個=0, 5個=2, 6個=3, 以降+2ずつ（最大10）

### 6. レベルシステム

- スコアに応じてレベルアップ（10段階）
- レベルが上がるほど落下速度が増加
- レベル毎の落下間隔: `800ms - (level - 1) × 70ms`（最低100ms）

### 7. UI / UX 要件

- **画面構成**:
  - 中央: ゲームボード
  - 右側: スコア / レベル / 連鎖数 / NEXTぷよ表示
  - 上部: ゲームタイトル・操作説明トグル
- **ビジュアル**:
  - ぷよは丸みのある形状でグラデーション＋光沢表現（CSS）
  - 隣接する同色ぷよは「くっつき表現」（接続方向に応じた形状変化）
  - 連鎖発生時にエフェクト（拡大・フラッシュ・パーティクル）
  - ダークモード対応（`prefers-color-scheme` 準拠）
- **アニメーション**:
  - ぷよ落下: スムーズなトランジション
  - 消去: フラッシュ → 縮小 → 消滅
  - 連鎖テキスト: 画面中央にポップアップ表示
  - ゲームオーバー: フェードアウト演出
- **レスポンシブ**: PC・タブレット・スマートフォン対応

### 8. ゲームモード（フェーズ1）

- **ひとりで遊ぶ（エンドレス）**: スコアを競う無限モード
- ハイスコアをローカルストレージに保存・表示

### 9. 将来拡張（フェーズ2以降）

- 対戦モード（おじゃまぷよ送受信）
- オンラインランキング
- BGM・効果音
- リプレイ機能
